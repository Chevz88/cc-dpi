pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS) // <--- Ini yang krusial!
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "cc-dpi"
include ':app'
